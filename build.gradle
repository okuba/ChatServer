apply plugin: 'java'

sourceCompatibility='1.8'
targetCompatibility='1.8'

repositories{
}

dependencies{
}

jar{
    manifest{
        attributes 'Main-Class': 'com.github.chat.ChatServer'
    }
    from configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
}

build << {
}